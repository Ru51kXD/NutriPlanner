# 🚀 Полная инструкция по запуску проекта NutriPlanner

## 📋 Что нужно сделать перед запуском

### 1. Проверка PostgreSQL

Убедитесь, что PostgreSQL установлен и запущен:

```powershell
# Проверка версии
psql --version

# Проверка службы (в PowerShell)
Get-Service | Where-Object {$_.Name -like "*postgresql*"}
```

Если PostgreSQL не запущен:
- Откройте "Службы" (Win+R → `services.msc`)
- Найдите `postgresql-x64-XX`
- Нажмите "Start" (Запустить)

### 2. Создание базы данных

**Вариант A: Через pgAdmin (рекомендуется)**

1. Откройте pgAdmin
2. Подключитесь к серверу PostgreSQL (localhost:5432)
3. Правой кнопкой на "Databases" → "Create" → "Database..."
4. Имя: `nutriplanner` → "Save"
5. Правой кнопкой на `nutriplanner` → "Query Tool"
6. Откройте файл `database/schema.sql` (Ctrl+O)
7. Выполните скрипт (F5)
8. Откройте файл `database/seed.sql` и выполните его (F5)

**Вариант B: Через командную строку**

```powershell
# Создание базы данных
psql -U postgres -c "CREATE DATABASE nutriplanner;"

# Создание таблиц
psql -U postgres -d nutriplanner -f database/schema.sql

# Заполнение данными
psql -U postgres -d nutriplanner -f database/seed.sql
```

---

## 🔧 Настройка Backend (Node.js)

### Шаг 1: Установка зависимостей

```powershell
cd backend
npm install
```

### Шаг 2: Создание файла .env

Создайте файл `.env` в папке `backend` со следующим содержимым:

```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nutriplanner
DB_USER=postgres
DB_PASSWORD=ВАШ_ПАРОЛЬ_ЗДЕСЬ
PORT=5000
```

**⚠️ ВАЖНО:** Замените `ВАШ_ПАРОЛЬ_ЗДЕСЬ` на пароль, который вы установили для пользователя `postgres` при установке PostgreSQL.

### Шаг 3: Проверка подключения

```powershell
npm start
```

Должно появиться:
```
✅ Подключение к PostgreSQL установлено
🚀 Сервер запущен на порту 5000
📡 API доступен по адресу: http://localhost:5000/api
```

Проверьте в браузере: http://localhost:5000/api/health

Должен вернуться JSON:
```json
{"status":"ok","database":"connected"}
```

---

## 🎨 Настройка Frontend

### Шаг 1: Установка зависимостей

```powershell
cd nutriplanner-frontend
npm install
```

### Шаг 2: Включение API

Файл `nutriplanner-frontend/src/utils/storage.js` уже настроен для работы с API. Убедитесь, что:

1. В строке 56 установлено: `const USE_API = true;`
2. Файл `nutriplanner-frontend/src/utils/api.js` существует (он уже есть)

---

## ▶️ Запуск проекта

### Вариант 1: Два отдельных терминала

**Терминал 1 - Backend:**
```powershell
cd backend
npm start
```

**Терминал 2 - Frontend:**
```powershell
cd nutriplanner-frontend
npm start
```

### Вариант 2: Один терминал (PowerShell)

```powershell
# Запуск backend в фоне
cd backend
Start-Process powershell -ArgumentList "-NoExit", "-Command", "npm start"

# Запуск frontend
cd ..\nutriplanner-frontend
npm start
```

---

## ✅ Проверка работы

1. **Backend должен быть доступен:**
   - http://localhost:5000/api/health
   - http://localhost:5000/api/users
   - http://localhost:5000/api/recipes

2. **Frontend должен быть доступен:**
   - http://localhost:3000 (автоматически откроется в браузере)

3. **Проверка связи Frontend ↔ Backend:**
   - Откройте консоль браузера (F12)
   - Зарегистрируйтесь или войдите в систему
   - Данные должны сохраняться в PostgreSQL

4. **Проверка данных в PostgreSQL:**
   ```powershell
   psql -U postgres -d nutriplanner
   ```
   ```sql
   SELECT * FROM users;
   SELECT COUNT(*) FROM recipes;
   \q
   ```

---

## 🔍 Устранение проблем

### Проблема: "Ошибка подключения к PostgreSQL"

**Решение:**
1. Проверьте, что PostgreSQL запущен:
   ```powershell
   Get-Service | Where-Object {$_.Name -like "*postgresql*"}
   ```
2. Проверьте пароль в файле `backend/.env`
3. Проверьте, что база данных `nutriplanner` создана:
   ```powershell
   psql -U postgres -l | Select-String "nutriplanner"
   ```

### Проблема: "CORS ошибка"

**Решение:**
- Backend уже настроен с CORS
- Если проблема сохраняется, проверьте, что backend запущен на порту 5000

### Проблема: "Порт 5000 занят"

**Решение:**
1. Измените `PORT=5000` на `PORT=5001` в `backend/.env`
2. Измените `API_BASE_URL` в `nutriplanner-frontend/src/utils/api.js` на `http://localhost:5001/api`

### Проблема: "Frontend не подключается к API"

**Решение:**
1. Убедитесь, что backend запущен
2. Проверьте URL в `nutriplanner-frontend/src/utils/api.js` (должен быть `http://localhost:5000/api`)
3. Проверьте, что в `storage.js` установлено `USE_API = true`
4. Откройте консоль браузера (F12) для просмотра ошибок

### Проблема: "База данных не найдена"

**Решение:**
1. Создайте базу данных (см. раздел "Создание базы данных")
2. Выполните скрипты `schema.sql` и `seed.sql`

---

## 📊 Структура проекта

```
planner/
├── backend/                 # Node.js Backend
│   ├── server.js           # Основной файл сервера
│   ├── package.json        # Зависимости
│   └── .env               # Конфигурация (создать!)
│
├── nutriplanner-frontend/  # React Frontend
│   ├── src/
│   │   ├── utils/
│   │   │   ├── api.js      # API клиент
│   │   │   └── storage.js  # Хранилище (USE_API = true)
│   │   └── ...
│   └── package.json
│
└── database/               # SQL скрипты
    ├── schema.sql         # Схема базы данных
    └── seed.sql           # Начальные данные
```

---

## 🎯 Быстрый старт (краткая версия)

```powershell
# 1. Создать БД (если еще не создана)
psql -U postgres -c "CREATE DATABASE nutriplanner;"
psql -U postgres -d nutriplanner -f database/schema.sql
psql -U postgres -d nutriplanner -f database/seed.sql

# 2. Настроить backend
cd backend
npm install
# Создать .env файл с паролем PostgreSQL
npm start

# 3. Настроить frontend (в другом терминале)
cd ..\nutriplanner-frontend
npm install
# Убедиться, что USE_API = true в storage.js
npm start
```

---

## ✅ Готово!

После выполнения всех шагов:
- ✅ База данных PostgreSQL настроена
- ✅ Backend работает на http://localhost:5000
- ✅ Frontend работает на http://localhost:3000
- ✅ Данные сохраняются в PostgreSQL
- ✅ Связь между frontend и backend установлена

---

## 📝 Дополнительная информация

- **Backend API:** http://localhost:5000/api
- **Frontend:** http://localhost:3000
- **База данных:** PostgreSQL на localhost:5432
- **Имя БД:** nutriplanner

Для подробной информации о работе с PostgreSQL см.:
- `PGADMIN_INSTRUCTIONS.md` - работа через pgAdmin
- `DATABASE_CONNECTION_GUIDE.md` - подробное руководство


