# Инструкция по работе с PostgreSQL через pgAdmin

## 📋 Что такое pgAdmin?

pgAdmin - это графический интерфейс для работы с PostgreSQL. Он намного проще, чем командная строка, особенно для начинающих.

## 🖥️ Как выглядит интерфейс

Когда вы откроете Query Tool, вы увидите:
- **Вверху:** Вкладки с подключениями и панель инструментов с кнопками
- **Слева:** Вкладки "Query" (для ввода SQL) и "Query History" (история запросов)
- **Справа:** Панель "Scratch Pad" (можно закрыть, нажав 'x')
- **В центре:** Большой редактор для ввода SQL запросов с номерами строк
- **Внизу:** Вкладки "Data Output" (результаты), "Messages" (сообщения), "Notifications" (уведомления)

---

## Шаг 1: Открытие pgAdmin

1. **Найдите pgAdmin в меню "Пуск"** Windows
   - Введите в поиске: `pgAdmin`
   - Или найдите в папке: `PostgreSQL 15` → `pgAdmin 4`

2. **При первом запуске** вас попросят установить мастер-пароль
   - Это пароль для защиты сохраненных паролей в pgAdmin
   - Запомните его или запишите в безопасном месте
   - Можно использовать тот же пароль, что и для PostgreSQL

---

## Шаг 2: Подключение к серверу PostgreSQL

1. **В левой панели** найдите "Servers"
   - Если сервер уже добавлен, он будет виден
   - Если нет - нужно добавить

2. **Добавление сервера (если нужно):**
   - Правой кнопкой мыши на "Servers" → "Register" → "Server..."
   - Или нажмите на иконку "Add New Server" (плюсик)

3. **Вкладка "General":**
   - **Name:** `PostgreSQL Server` (или любое имя)
   - Нажмите "Save"

4. **Вкладка "Connection":**
   - **Host name/address:** `localhost`
   - **Port:** `5432`
   - **Maintenance database:** `postgres`
   - **Username:** `postgres`
   - **Password:** Введите пароль, который вы установили при установке PostgreSQL
   - ✅ Поставьте галочку "Save password" (чтобы не вводить каждый раз)
   - Нажмите "Save"

5. **Если подключение успешно:**
   - Сервер появится в левой панели
   - Рядом с ним будет зеленая иконка (сервер работает)

---

## Шаг 3: Создание базы данных

1. **Разверните сервер** в левой панели (нажмите на стрелочку)
   - Вы увидите: Databases, Login/Group Roles, Tablespaces

2. **Правой кнопкой мыши** на "Databases" → "Create" → "Database..."

3. **В открывшемся окне:**
   - **Database:** `nutriplanner`
   - **Owner:** `postgres` (оставьте по умолчанию)
   - **Encoding:** `UTF8` (обычно уже выбрано)
   - Остальные настройки оставьте по умолчанию
   - Нажмите "Save"

4. **База данных создана!**
   - Она появится в списке баз данных
   - Разверните её, чтобы увидеть структуру

---

## Шаг 4: Создание таблиц (выполнение SQL скрипта)

### Вариант A: Через Query Tool (рекомендуется)

1. **Правой кнопкой мыши** на базе данных `nutriplanner` → "Query Tool"
   - Откроется окно с темным интерфейсом
   - Вверху вы увидите вкладку с названием вашей базы данных (например, "postgres/postgres@localhost")
   - В центре будет большая область для ввода SQL запросов

2. **Откройте файл со схемой:**
   - В верхнем меню найдите иконку папки с выпадающим меню (📁) → "Open File"
   - Или используйте горячую клавишу: `Ctrl + O`
   - Найдите файл: `database/schema.sql` (в папке вашего проекта)
   - Выберите файл и нажмите "Open"

3. **Проверьте SQL код:**
   - В левой части окна (вкладка "Query") вы увидите весь SQL код
   - Код будет с номерами строк слева
   - Убедитесь, что в начале есть: `CREATE EXTENSION IF NOT EXISTS "uuid-ossp";`

4. **Выполните скрипт:**
   - Найдите кнопку выполнения запроса в верхней панели инструментов
   - Это кнопка с треугольником "▶️" (Play/Execute)
   - Или нажмите горячую клавишу: `F5`
   - Или `Ctrl + Enter`

5. **Проверьте результат:**
   - Внизу окна перейдите на вкладку "Messages"
   - Должно быть сообщение: "Successfully run. Total query runtime: ..."
   - Если есть ошибки - они будут показаны красным цветом во вкладке "Messages"
   - Во вкладке "Data Output" может быть сообщение "No data output" - это нормально для CREATE запросов

6. **Проверьте созданные таблицы:**
   - В левой панели (дерево объектов) разверните: `nutriplanner` → `Schemas` → `public` → `Tables`
   - Вы должны увидеть таблицы:
     - `users`
     - `recipes`
     - `diseases`
     - `diet_plans`
     - `user_progress`
   - Если таблиц нет - обновите дерево объектов (правой кнопкой на "Tables" → "Refresh")

### Вариант B: Копирование SQL кода вручную

1. **Откройте файл** `database/schema.sql` в любом текстовом редакторе (Notepad, VS Code)

2. **Скопируйте весь код:**
   - Выделите весь текст: `Ctrl + A`
   - Скопируйте: `Ctrl + C`

3. **В Query Tool:**
   - Правой кнопкой на `nutriplanner` → "Query Tool"
   - Кликните в область редактора запросов (вкладка "Query")
   - Вставьте код: `Ctrl + V`
   - Нажмите кнопку выполнения (▶️) или `F5`

---

## Шаг 5: Заполнение начальными данными

1. **Откройте Query Tool:**
   - Правой кнопкой мыши на базе данных `nutriplanner` → "Query Tool"
   - Или если Query Tool уже открыт - можно использовать его

2. **Откройте файл** `database/seed.sql`:
   - В верхней панели найдите иконку папки (📁) → "Open File"
   - Или нажмите `Ctrl + O`
   - Найдите и выберите файл `database/seed.sql` из папки проекта
   - Нажмите "Open"

3. **Проверьте SQL код:**
   - В редакторе запросов (вкладка "Query") вы увидите INSERT запросы
   - Убедитесь, что код загрузился полностью

4. **Выполните скрипт:**
   - Нажмите кнопку выполнения (▶️) в верхней панели
   - Или нажмите `F5`
   - Или `Ctrl + Enter`

5. **Проверьте результат:**
   - Перейдите на вкладку "Messages" внизу окна
   - Должно быть сообщение об успешном выполнении
   - Если есть ошибки - они будут показаны красным

6. **Проверьте данные:**
   - В левой панели (дерево объектов) найдите таблицу `recipes`
   - Правой кнопкой на `recipes` → "View/Edit Data" → "First 100 Rows"
   - Вы должны увидеть несколько рецептов в таблице
   - Аналогично проверьте таблицу `diseases`

---

## Шаг 6: Просмотр и редактирование данных

### Просмотр данных таблицы:

1. **Правой кнопкой мыши** на таблице (например, `users`)
2. Выберите: "View/Edit Data" → "First 100 Rows"
3. Вы увидите все записи в таблице

### Добавление данных вручную:

1. Откройте таблицу: "View/Edit Data" → "First 100 Rows"
2. Внизу нажмите кнопку "Add Row" (➕)
3. Заполните поля
4. Нажмите "Save" (💾)

### Редактирование данных:

1. Откройте таблицу
2. Дважды кликните на ячейку, которую хотите изменить
3. Введите новое значение
4. Нажмите "Save"

### Удаление данных:

1. Откройте таблицу
2. Выделите строку (кликните на номер строки слева)
3. Нажмите кнопку "Delete Row" (🗑️)
4. Подтвердите удаление

---

## Шаг 7: Выполнение SQL запросов

### Простые запросы для проверки:

1. **Откройте Query Tool:**
   - Правой кнопкой на `nutriplanner` → "Query Tool"
   - Убедитесь, что вверху выбрана правильная база данных (должно быть "nutriplanner/...")

2. **Просмотр всех пользователей:**
   - В редакторе запросов (вкладка "Query") введите:
   ```sql
   SELECT * FROM users;
   ```
   - Нажмите кнопку выполнения (▶️) или `F5`
   - Результаты появятся во вкладке "Data Output" внизу окна

3. **Просмотр всех рецептов:**
   - Введите в редакторе:
   ```sql
   SELECT name, calories, image_url FROM recipes LIMIT 10;
   ```
   - Нажмите `F5` для выполнения
   - Результаты будут во вкладке "Data Output"

4. **Подсчет записей:**
   - Выполните по очереди:
   ```sql
   SELECT COUNT(*) FROM recipes;
   SELECT COUNT(*) FROM users;
   SELECT COUNT(*) FROM diseases;
   ```
   - Каждый запрос покажет количество записей в таблице

5. **Поиск по условию:**
   ```sql
   SELECT * FROM users WHERE email = 'test@example.com';
   ```

### Полезные функции Query Tool:

- **Выполнить выделенный запрос:**
  - Выделите часть SQL кода мышкой
  - Нажмите `F5` - выполнится только выделенный код

- **Очистить редактор:**
  - Нажмите `Ctrl + Shift + C` или найдите кнопку очистки в панели инструментов

- **История запросов:**
  - Перейдите на вкладку "Query History" слева
  - Там будут все ранее выполненные запросы

---

## Шаг 8: Проверка структуры таблицы

1. **Правой кнопкой мыши** на таблице → "Properties"
2. Перейдите на вкладку "Columns"
3. Вы увидите:
   - Названия колонок
   - Типы данных
   - Ограничения (NOT NULL, PRIMARY KEY и т.д.)

---

## Шаг 9: Резервное копирование (Backup)

### Создание резервной копии:

1. **Правой кнопкой мыши** на базе данных `nutriplanner`
2. Выберите: "Backup..."
3. В окне:
   - **Filename:** выберите место сохранения (например, `C:\Backup\nutriplanner_backup.sql`)
   - **Format:** `Plain` (для SQL файла) или `Custom` (для сжатого)
   - Нажмите "Backup"
4. Дождитесь завершения (внизу будет прогресс)

### Восстановление из резервной копии:

1. **Правой кнопкой мыши** на базе данных `nutriplanner`
2. Выберите: "Restore..."
3. В окне:
   - **Filename:** выберите файл резервной копии
   - Нажмите "Restore"
4. Дождитесь завершения

---

## Шаг 10: Удаление базы данных (если нужно пересоздать)

⚠️ **ВНИМАНИЕ:** Это удалит все данные!

1. **Правой кнопкой мыши** на базе данных `nutriplanner`
2. Выберите: "Delete/Drop"
3. Подтвердите удаление
4. Затем создайте базу заново (см. Шаг 3)

---

## Частые проблемы и решения

### Проблема: "Не могу подключиться к серверу"

**Решение:**
1. Проверьте, что PostgreSQL запущен:
   - Откройте "Службы" (Win+R → `services.msc`)
   - Найдите `postgresql-x64-XX`
   - Убедитесь, что статус "Running"
   - Если нет - нажмите "Start"

2. Проверьте параметры подключения:
   - Host: `localhost`
   - Port: `5432`
   - Username: `postgres`
   - Password: правильный ли пароль?

### Проблема: "Ошибка при выполнении SQL"

**Решение:**
1. Проверьте, что вы подключены к правильной базе данных (`nutriplanner`)
2. Убедитесь, что таблицы уже созданы (если выполняете запросы к ним)
3. Проверьте синтаксис SQL (запятые, кавычки, скобки)

### Проблема: "Permission denied"

**Решение:**
1. Убедитесь, что вы подключены как пользователь `postgres` (это администратор)
2. Если используете другого пользователя - дайте ему права:
   ```sql
   GRANT ALL PRIVILEGES ON DATABASE nutriplanner TO ваш_пользователь;
   ```

### Проблема: "База данных уже существует"

**Решение:**
- Либо используйте существующую базу
- Либо удалите старую (Шаг 10) и создайте новую

---

## Полезные горячие клавиши в Query Tool

- **F5** или **Ctrl + Enter** - Выполнить запрос
- **Ctrl + Space** - Автодополнение (подсказки при вводе)
- **Ctrl + /** - Закомментировать/раскомментировать строку
- **Ctrl + Shift + C** - Очистить окно запросов
- **Ctrl + O** - Открыть файл
- **Ctrl + S** - Сохранить запрос
- **Ctrl + F** - Поиск в редакторе
- **Ctrl + H** - Замена текста
- **Alt + Shift + ↑/↓** - Переместить строку вверх/вниз
- **Ctrl + D** - Дублировать строку

---

## Что дальше?

После настройки базы данных в pgAdmin:

1. ✅ База данных создана
2. ✅ Таблицы созданы
3. ✅ Начальные данные загружены
4. ➡️ Теперь настройте backend (см. `DATABASE_CONNECTION_GUIDE.md`)
5. ➡️ Запустите backend сервер
6. ➡️ Включите API в frontend (измените `USE_API = true` в `storage.js`)

---

## Визуальная структура pgAdmin

```
Servers
└── PostgreSQL Server (localhost:5432)
    └── Databases
        └── nutriplanner ← Ваша база данных
            └── Schemas
                └── public
                    └── Tables
                        ├── users
                        ├── recipes
                        ├── diseases
                        ├── diet_plans
                        └── user_progress
```

---

## Дополнительные возможности pgAdmin

### Просмотр зависимостей таблицы:
- Правой кнопкой на таблице → "Dependencies"

### Просмотр зависимых объектов:
- Правой кнопкой на таблице → "Dependents"

### Экспорт данных в CSV:
- Правой кнопкой на таблице → "Export/Import" → "Export..."

### Создание диаграммы базы данных:
- Правой кнопкой на базе данных → "Generate ERD" (требует установки дополнительных инструментов)

---

## Готово! 🎉

Теперь вы умеете работать с PostgreSQL через pgAdmin. Это намного проще, чем командная строка!

Если возникнут вопросы - проверьте раздел "Частые проблемы и решения" выше.

