# ⚡ Быстрый запуск NutriPlanner

## 📋 Предварительные требования

1. ✅ PostgreSQL установлен и запущен
2. ✅ База данных `nutriplanner` создана
3. ✅ Таблицы созданы (выполнен `database/schema.sql`)
4. ✅ Начальные данные загружены (выполнен `database/seed.sql`)

## 🚀 Запуск за 3 шага

### Шаг 1: Настройка Backend

```powershell
cd backend
npm install

# Создайте файл .env со следующим содержимым:
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=nutriplanner
# DB_USER=postgres
# DB_PASSWORD=ВАШ_ПАРОЛЬ_ЗДЕСЬ
# PORT=5000

npm start
```

**Ожидаемый результат:**
```
✅ Подключение к PostgreSQL установлено
🚀 Сервер запущен на порту 5000
```

### Шаг 2: Настройка Frontend

```powershell
# В новом терминале
cd nutriplanner-frontend
npm install
npm start
```

**Ожидаемый результат:**
- Браузер откроется автоматически на http://localhost:3000

### Шаг 3: Проверка

1. **Backend:** http://localhost:5000/api/health
   - Должен вернуть: `{"status":"ok","database":"connected"}`

2. **Frontend:** http://localhost:3000
   - Должен открыться интерфейс приложения

3. **Проверка связи:**
   - Зарегистрируйтесь в приложении
   - Данные должны сохраниться в PostgreSQL

## ⚠️ Если что-то не работает

### Backend не запускается
- Проверьте, что PostgreSQL запущен
- Проверьте пароль в `.env`
- Проверьте, что база данных `nutriplanner` существует

### Frontend не подключается к API
- Убедитесь, что backend запущен
- Проверьте консоль браузера (F12) на наличие ошибок
- Убедитесь, что в `storage.js` установлено `USE_API = true`

### База данных не найдена
```powershell
# Создайте базу данных
psql -U postgres -c "CREATE DATABASE nutriplanner;"
psql -U postgres -d nutriplanner -f database/schema.sql
psql -U postgres -d nutriplanner -f database/seed.sql
```

## 📚 Подробная инструкция

См. файл `LAUNCH_GUIDE.md` для полной инструкции.


