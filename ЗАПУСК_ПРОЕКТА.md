# 🚀 Инструкция по запуску проекта NutriPlanner

## ✅ Что уже сделано

1. ✅ Файл `.env` создан в папке `backend/` (нужно указать пароль PostgreSQL)
2. ✅ Frontend настроен для работы с API (`USE_API = true`)
3. ✅ API клиент настроен и готов к работе

## 📋 Что нужно сделать

### 1. Указать пароль PostgreSQL

Откройте файл `backend/.env` и укажите ваш пароль PostgreSQL:

```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nutriplanner
DB_USER=postgres
DB_PASSWORD=ВАШ_ПАРОЛЬ_ЗДЕСЬ  ← Замените на ваш пароль
PORT=5000
```

### 2. Убедиться, что база данных создана

Если база данных еще не создана, выполните:

**Через pgAdmin (рекомендуется):**
1. Откройте pgAdmin
2. Подключитесь к серверу PostgreSQL
3. Правой кнопкой на "Databases" → "Create" → "Database..."
4. Имя: `nutriplanner` → "Save"
5. Правой кнопкой на `nutriplanner` → "Query Tool"
6. Откройте `database/schema.sql` (Ctrl+O) и выполните (F5)
7. Откройте `database/seed.sql` и выполните (F5)

**Через командную строку:**
```powershell
psql -U postgres -c "CREATE DATABASE nutriplanner;"
psql -U postgres -d nutriplanner -f database/schema.sql
psql -U postgres -d nutriplanner -f database/seed.sql
```

### 3. Запустить Backend

```powershell
cd backend
npm install
npm start
```

**Ожидаемый результат:**
```
✅ Подключение к PostgreSQL установлено
🚀 Сервер запущен на порту 5000
📡 API доступен по адресу: http://localhost:5000/api
```

**Проверка:** Откройте в браузере http://localhost:5000/api/health
- Должен вернуться: `{"status":"ok","database":"connected"}`

### 4. Запустить Frontend

Откройте **новый терминал** (не закрывайте backend):

```powershell
cd nutriplanner-frontend
npm install
npm start
```

**Ожидаемый результат:**
- Браузер автоматически откроется на http://localhost:3000
- Интерфейс приложения должен загрузиться

## 🔍 Проверка работы

1. **Backend работает:**
   - http://localhost:5000/api/health → `{"status":"ok","database":"connected"}`
   - http://localhost:5000/api/users → список пользователей
   - http://localhost:5000/api/recipes → список рецептов

2. **Frontend работает:**
   - http://localhost:3000 → интерфейс приложения

3. **Связь Frontend ↔ Backend:**
   - Зарегистрируйтесь в приложении
   - Откройте консоль браузера (F12) → вкладка "Network"
   - Должны быть запросы к `http://localhost:5000/api/...`
   - Данные должны сохраняться в PostgreSQL

4. **Проверка данных в PostgreSQL:**
   ```powershell
   psql -U postgres -d nutriplanner
   ```
   ```sql
   SELECT * FROM users;
   SELECT COUNT(*) FROM recipes;
   \q
   ```

## ⚠️ Решение проблем

### Ошибка: "Ошибка подключения к PostgreSQL"

**Причины:**
- PostgreSQL не запущен
- Неправильный пароль в `.env`
- База данных `nutriplanner` не создана

**Решение:**
1. Проверьте службу PostgreSQL:
   ```powershell
   Get-Service | Where-Object {$_.Name -like "*postgresql*"}
   ```
2. Проверьте пароль в `backend/.env`
3. Создайте базу данных (см. шаг 2 выше)

### Ошибка: "CORS error" в браузере

**Решение:**
- Backend уже настроен с CORS
- Убедитесь, что backend запущен на порту 5000
- Проверьте URL в `nutriplanner-frontend/src/utils/api.js`

### Ошибка: "Порт 5000 занят"

**Решение:**
1. Измените `PORT=5000` на `PORT=5001` в `backend/.env`
2. Измените `API_BASE_URL` в `nutriplanner-frontend/src/utils/api.js` на `http://localhost:5001/api`
3. Перезапустите backend

### Frontend не подключается к API

**Решение:**
1. Убедитесь, что backend запущен
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Убедитесь, что в `nutriplanner-frontend/src/utils/storage.js` установлено `USE_API = true`
4. Проверьте URL в `nutriplanner-frontend/src/utils/api.js` (должен быть `http://localhost:5000/api`)

## 📊 Структура проекта

```
planner/
├── backend/                    # Node.js Backend
│   ├── server.js              # Основной файл сервера
│   ├── package.json           # Зависимости
│   └── .env                   # Конфигурация (создан, нужно указать пароль)
│
├── nutriplanner-frontend/      # React Frontend
│   ├── src/
│   │   ├── utils/
│   │   │   ├── api.js         # API клиент (готов)
│   │   │   └── storage.js     # Хранилище (USE_API = true)
│   │   └── ...
│   └── package.json
│
└── database/                   # SQL скрипты
    ├── schema.sql             # Схема базы данных
    └── seed.sql               # Начальные данные
```

## 🎯 Быстрый старт (если всё уже настроено)

```powershell
# Терминал 1 - Backend
cd backend
npm start

# Терминал 2 - Frontend
cd nutriplanner-frontend
npm start
```

## ✅ Готово!

После выполнения всех шагов:
- ✅ База данных PostgreSQL настроена
- ✅ Backend работает на http://localhost:5000
- ✅ Frontend работает на http://localhost:3000
- ✅ Данные сохраняются в PostgreSQL
- ✅ Связь между frontend и backend установлена

---

## 📚 Дополнительная информация

- **Подробная инструкция:** `LAUNCH_GUIDE.md`
- **Быстрый старт:** `QUICK_START.md`
- **Работа с PostgreSQL через pgAdmin:** `PGADMIN_INSTRUCTIONS.md`
- **Подключение к базе данных:** `DATABASE_CONNECTION_GUIDE.md`

