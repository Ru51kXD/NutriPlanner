# 🌱 NutriPlanner - Система планирования питания

## 📋 Описание проекта

NutriPlanner — это веб-приложение для формирования индивидуальных рационов питания с учётом медицинских ограничений. Система позволяет пользователям создавать персонализированные планы питания на основе их заболеваний, целей (похудение/набор массы) и доступных продуктов.

## 🚀 Основные возможности

- **Генерация индивидуальных рационов** с учётом 35+ заболеваний
- **AI-поиск рецептов** по доступным продуктам (100+ рецептов)
- **Сезонное меню** с рекомендациями по временам года
- **Отслеживание прогресса** с визуализацией данных
- **Управление профилем** с указанием заболевания и цели
- **Админ-панель** для управления данными

## 🛠 Технологический стек

### Frontend
- **React 18.2.0** — библиотека для создания пользовательского интерфейса
- **Material-UI (MUI) 5.18.0** — компонентная библиотека для UI
- **React Router DOM 6.15.0** — маршрутизация
- **Recharts 3.3.0** — визуализация данных (графики)
- **localStorage** — хранение данных на клиенте

### Backend (не используется в текущей версии)
- **Java Spring Boot** — фреймворк для бэкенда (заменён на localStorage)

## 📁 Структура проекта

```
planner/
├── nutriplanner-frontend/          # Frontend приложение
│   ├── src/
│   │   ├── components/            # Переиспользуемые компоненты
│   │   │   ├── AppHeader.js       # Шапка сайта с навигацией
│   │   │   ├── AppFooter.js       # Подвал сайта
│   │   │   └── ScrollToTop.js     # Компонент для прокрутки вверх
│   │   ├── views/                 # Страницы приложения
│   │   │   ├── Dashboard.js       # Главная страница
│   │   │   ├── GeneratorPage.js   # Генерация рационов
│   │   │   ├── RecipeGeneratorPage.js  # AI-поиск рецептов
│   │   │   ├── SeasonalMenuPage.js     # Сезонное меню
│   │   │   ├── ProfilePage.js     # Профиль пользователя
│   │   │   ├── ProgressPage.js    # Страница прогресса
│   │   │   ├── PlansPage.js       # Список планов
│   │   │   ├── LoginPage.js       # Вход
│   │   │   ├── RegisterPage.js    # Регистрация
│   │   │   └── AdminPage.js       # Админ-панель
│   │   ├── utils/
│   │   │   └── storage.js         # Сервис для работы с localStorage
│   │   ├── router/
│   │   │   ├── index.js           # Маршруты приложения
│   │   │   └── guards.js          # Защита маршрутов (AuthGuard, GuestGuard)
│   │   ├── App.js                 # Главный компонент приложения
│   │   └── index.js               # Точка входа
│   └── package.json
└── nutriplanner-backend/          # Backend (не используется)
```

## 🎯 Функционал страниц

### 1. Главная страница (Dashboard)
**Файл:** `src/views/Dashboard.js`

**Функционал:**
- Приветственный экран с описанием возможностей
- Кнопки быстрого доступа к основным функциям
- Плавная анимация и современный дизайн

**Особенности:**
- Градиентный фон с анимацией
- Плавная прокрутка при переходе на другие страницы

---

### 2. Генерация рационов (GeneratorPage)
**Файл:** `src/views/GeneratorPage.js`

**Функционал:**
- Создание индивидуальных рационов питания
- Учёт персональных данных (возраст, вес, рост, пол)
- Выбор заболевания из 35+ вариантов
- Выбор цели (похудение/набор массы/поддержание)
- Генерация плана на неделю с разными блюдами каждый день

#### Алгоритм генерации рациона

**Шаг 1: Расчёт базового метаболизма (BMR)**
```javascript
// Формула Миффлина-Сан Жеора
BMR = (10 × вес) + (6.25 × рост) - (5 × возраст) + 5  // для мужчин
BMR = (10 × вес) + (6.25 × рост) - (5 × возраст) - 161  // для женщин
```

**Шаг 2: Учёт уровня активности**
```javascript
const multipliers = {
  sedentary: 1.2,      // Сидячий образ жизни
  light: 1.375,        // Лёгкая активность
  moderate: 1.55,      // Умеренная активность
  high: 1.725,         // Высокая активность
  athlete: 1.9         // Профессиональный спорт
};

TDEE = BMR × multiplier
```

**Шаг 3: Корректировка по заболеванию**
- Каждое заболевание имеет свой план питания с:
  - Целевыми калориями
  - Распределением БЖУ (белки, жиры, углеводы)
  - Рекомендуемыми витаминами
  - Базовыми блюдами для каждого приёма пищи

**Шаг 4: Генерация меню на неделю**
```javascript
// Для каждого дня недели:
1. Берётся базовый план для заболевания
2. Генерируются вариации блюд для каждого приёма пищи
3. Обеспечивается разнообразие (разные блюда каждый день)
4. Рассчитываются калории и БЖУ для каждого дня
```

**Пример структуры плана:**
```javascript
{
  day: 'Понедельник',
  meals: [
    { type: 'Завтрак', name: 'Овсяная каша', calories: 400, protein: 18, fat: 12, carbs: 60 },
    { type: 'Перекус', name: 'Банан', calories: 200, protein: 8, fat: 5, carbs: 30 },
    { type: 'Обед', name: 'Суп-пюре', calories: 600, protein: 25, fat: 20, carbs: 80 },
    { type: 'Ужин', name: 'Манная каша', calories: 400, protein: 20, fat: 10, carbs: 55 }
  ]
}
```

**Особенности:**
- Автозаполнение данных из профиля пользователя
- Валидация обязательных полей
- Сохранение плана в localStorage
- Отображение плана с возможностью просмотра по дням

---

### 3. AI-поиск рецептов (RecipeGeneratorPage)
**Файл:** `src/views/RecipeGeneratorPage.js`

**Функционал:**
- Поиск рецептов по списку доступных продуктов
- База из 100 рецептов
- Умный алгоритм сопоставления ингредиентов
- Отображение БЖУ и калорий для каждого рецепта

#### Алгоритм поиска рецептов

**Шаг 1: Нормализация продуктов**
```javascript
normalizeProduct(product) {
  return product.toLowerCase().trim();
}
```

**Шаг 2: Расширение синонимов**
Система использует словарь синонимов для улучшения поиска:
```javascript
const synonyms = {
  'яйца': ['яйцо', 'яичный', 'яичница', 'омлет'],
  'курица': ['куриное', 'куриный', 'куриная грудка', 'куриное филе'],
  'мясо': ['говядина', 'свинина', 'баранина', 'фарш'],
  // ... и т.д.
};
```

**Шаг 3: Весовая система оценки совпадений**
```javascript
// Для каждого рецепта:
1. Проверяется каждый ингредиент рецепта
2. Сравнивается с каждым продуктом пользователя (с учётом синонимов)
3. Присваивается вес совпадения:
   - Точное совпадение = 10 баллов
   - Полное вхождение = 8 баллов
   - Частичное совпадение = 4 балла
4. Рассчитывается финальный счёт:
   finalScore = matchScore + bonusScore + quantityBonus
   где:
   - matchScore — сумма весов всех совпадений
   - bonusScore — бонус за процент совпадения ингредиентов (до 5 баллов)
   - quantityBonus — бонус за количество совпадений (2 балла за каждое)
```

**Шаг 4: Сортировка результатов**
```javascript
// Сортировка по приоритету:
1. По финальному счёту (больше = лучше)
2. По количеству совпадений ингредиентов
3. По проценту совпадения
```

**Особенности:**
- Поиск работает даже с 1 совпадением ингредиента
- Учитываются множественные формы слов (яйца/яйцо)
- Поддержка ввода нескольких продуктов через запятую
- Ограничение: максимум 20 продуктов, максимум 15 для поиска

---

### 4. Сезонное меню (SeasonalMenuPage)
**Файл:** `src/views/SeasonalMenuPage.js`

**Функционал:**
- Выбор сезона (весна, лето, осень, зима)
- Отображение рецептов для выбранного сезона
- Детальная информация о каждом блюде

**Структура данных:**
```javascript
const seasonalRecipes = {
  spring: [...],  // 8 рецептов
  summer: [...],  // 10 рецептов
  autumn: [...],  // 10 рецептов
  winter: [...]   // 10 рецептов
};
```

**Особенности:**
- Каждый рецепт содержит: название, описание, калории, БЖУ, ингредиенты
- Модальное окно для детального просмотра рецепта
- Красивый дизайн с иконками сезонов

---

### 5. Профиль пользователя (ProfilePage)
**Файл:** `src/views/ProfilePage.js`

**Функционал:**
- Просмотр данных профиля
- Редактирование профиля
- Обязательные поля: основное заболевание, цель (похудение/набор массы)
- Отображение статистики (количество созданных планов)

**Особенности:**
- Модальное окно для редактирования
- Валидация обязательных полей
- Сохранение в localStorage и StorageService
- Отображение выбранного заболевания и цели

---

### 6. Страница прогресса (ProgressPage)
**Файл:** `src/views/ProgressPage.js`

**Функционал:**
- Отображение статистики по созданным планам
- Средние значения БЖУ
- Процент соблюдения плана
- Визуализация данных с помощью графиков

**Алгоритм расчёта прогресса:**
```javascript
1. Загружаются все планы пользователя
2. Рассчитывается среднее БЖУ:
   avgProtein = sum(protein) / plans.length
   avgFat = sum(fat) / plans.length
   avgCarbs = sum(carbs) / plans.length
3. Рассчитывается процент соблюдения (на основе сравнения с целевыми значениями)
```

---

### 7. Админ-панель (AdminPage)
**Файл:** `src/views/AdminPage.js`

**Функционал:**
- Управление пользователями (создание, редактирование, удаление)
- Управление рационами питания
- Управление заболеваниями
- Авторизация: логин `admin`, пароль `nimda`

**Особенности:**
- Отдельная система авторизации (не связана с обычной)
- Работа с данными через StorageService
- Таблицы с данными и модальные окна для редактирования

---

## 💾 Архитектура данных

### StorageService
**Файл:** `src/utils/storage.js`

**Назначение:** Централизованное управление данными в localStorage

**Основные методы:**

1. **Пользователи:**
   - `getUsers()` — получить всех пользователей
   - `saveUser(user)` — сохранить пользователя
   - `updateUser(userId, updatedUser)` — обновить пользователя
   - `deleteUser(userId)` — удалить пользователя

2. **Планы питания:**
   - `getPlans()` — получить все планы
   - `savePlan(plan)` — сохранить план
   - `updatePlan(planId, updatedPlan)` — обновить план
   - `deletePlan(planId)` — удалить план

3. **Заболевания:**
   - `getDiseases()` — получить все заболевания
   - `saveDisease(disease)` — сохранить заболевание
   - `updateDisease(diseaseId, updatedDisease)` — обновить заболевание
   - `deleteDisease(diseaseId)` — удалить заболевание

4. **Рецепты:**
   - `getRecipes()` — получить все рецепты
   - `saveRecipe(recipe)` — сохранить рецепт
   - `updateRecipe(recipeId, updatedRecipe)` — обновить рецепт
   - `deleteRecipe(recipeId)` — удалить рецепт

5. **Инициализация:**
   - `initializeDefaultData()` — заполнение начальными данными:
     - 3 тестовых пользователя
     - 5 тестовых планов питания
     - 10 заболеваний
     - 100 рецептов

### Структура данных в localStorage

```javascript
{
  'users': [...],           // Массив пользователей
  'dietPlans': [...],      // Массив планов питания
  'diseases': [...],        // Массив заболеваний
  'recipes': [...],         // Массив рецептов
  'appInitialized': true,  // Флаг инициализации
  
  // Данные текущего пользователя:
  'isAuthenticated': 'true',
  'userId': 'user1',
  'userName': 'Имя пользователя',
  'userEmail': 'email@example.com',
  'userAge': '25',
  'userWeight': '70',
  'userHeight': '175',
  'userDisease': 'gastritis',
  'userGoal': 'weight_loss',
  
  // Данные админа:
  'adminAuthenticated': 'true'
}
```

---

## 🔐 Система авторизации

### Обычная авторизация
- **Регистрация:** создание нового пользователя с обязательными полями (заболевание, цель)
- **Вход:** проверка email и пароля
- **Хранение:** статус авторизации в localStorage
- **Защита маршрутов:** `AuthGuard` и `GuestGuard` в `src/router/guards.js`

### Админ-авторизация
- **Логин:** `admin`
- **Пароль:** `nimda`
- **Хранение:** отдельный флаг `adminAuthenticated` в localStorage
- **Доступ:** только к админ-панели

---

## 🎨 Дизайн и анимации

### Цветовая схема
- **Основной цвет:** #10B981 (зелёный)
- **Вторичный цвет:** #3B82F6 (синий)
- **Ошибки:** #EF4444 (красный)
- **Предупреждения:** #F59E0B (оранжевый)

### Анимации
- **fadeInUp** — появление элементов снизу вверх
- **fadeIn** — плавное появление
- **slideInRight** — появление справа
- **scaleIn** — масштабирование
- **Градиентный фон** — анимированный градиент на главной странице

### Плавная прокрутка
- Автоматическая прокрутка вверх при переходе между страницами
- Плавные переходы при навигации

---

## 📊 Алгоритмы и формулы

### 1. Расчёт калорий (BMR/TDEE)

**Формула Миффлина-Сан Жеора:**
```
BMR (мужчины) = 10 × вес(кг) + 6.25 × рост(см) - 5 × возраст(лет) + 5
BMR (женщины) = 10 × вес(кг) + 6.25 × рост(см) - 5 × возраст(лет) - 161

TDEE = BMR × коэффициент активности
```

**Коэффициенты активности:**
- Сидячий образ жизни: 1.2
- Лёгкая активность: 1.375
- Умеренная активность: 1.55
- Высокая активность: 1.725
- Профессиональный спорт: 1.9

### 2. Алгоритм поиска рецептов

**Псевдокод:**
```
Для каждого рецепта в базе:
  matchScore = 0
  matchedIngredients = []
  
  Для каждого ингредиента рецепта:
    Для каждого продукта пользователя:
      synonyms = getSynonyms(product)
      
      Для каждого синонима:
        если точное совпадение:
          weight = 10
        иначе если полное вхождение:
          weight = 8
        иначе если частичное совпадение:
          weight = 4
        
        matchScore += weight
        matchedIngredients += ingredient
  
  bonusScore = (matchedIngredients.length / recipe.ingredients.length) × 5
  quantityBonus = matchedIngredients.length × 2
  finalScore = matchScore + bonusScore + quantityBonus

Сортировать рецепты по finalScore (по убыванию)
```

### 3. Генерация вариаций блюд

**Алгоритм:**
```javascript
// Для каждого дня недели:
1. Берётся базовое блюдо из плана заболевания
2. Выбирается вариация из массива вариантов:
   variation = variations[dayIndex % variations.length]
3. Обеспечивается разнообразие за счёт циклического перебора
```

---

## 🚀 Инструкция по запуску

### Требования
- Node.js 14+ 
- npm или yarn

### Установка и запуск

1. **Перейти в директорию frontend:**
```bash
cd nutriplanner-frontend
```

2. **Установить зависимости:**
```bash
npm install
```

3. **Запустить приложение:**
```bash
npm start
```

4. **Открыть в браузере:**
```
http://localhost:3000
```

### Первый запуск
При первом запуске автоматически инициализируются начальные данные:
- 3 тестовых пользователя
- 5 тестовых планов
- 10 заболеваний
- 100 рецептов

---

## 📝 Тестовые данные

### Тестовые пользователи
1. **Каролина Миллер**
   - Email: miller@mail.ru
   - Пароль: 123456
   - Возраст: 28, Вес: 65, Рост: 170

2. **Дмитрий Тумаев**
   - Email: tumaev@mail.ru
   - Пароль: 123456
   - Возраст: 35, Вес: 80, Рост: 180

3. **Анна Петрова**
   - Email: petrova@mail.ru
   - Пароль: 123456
   - Возраст: 32, Вес: 70, Рост: 165

### Админ-доступ
- **Логин:** admin
- **Пароль:** nimda

---

## 🔧 Настройка и кастомизация

### Добавление нового заболевания
1. Открыть `src/views/GeneratorPage.js`
2. Добавить запись в массив `diseases`
3. Добавить план в объект `diseasePlans`:
```javascript
newDisease: {
  goal: 'описание цели',
  bzu: { protein: 90, fat: 70, carbs: 350 },
  calories: 2400,
  meals: {
    breakfast: { name: '...', description: '...', calories: 400, ... },
    snack1: { ... },
    lunch: { ... },
    dinner: { ... }
  },
  vitamins: 'A, E, B2, B6'
}
```

### Добавление новых рецептов
1. Открыть `src/utils/storage.js`
2. Добавить рецепт в массив `defaultRecipes` в функции `initializeDefaultData()`
3. Формат рецепта:
```javascript
{
  id: 'recipe101',
  name: 'Название блюда',
  ingredients: ['ингредиент1', 'ингредиент2', ...],
  calories: 300,
  protein: 20,
  fat: 15,
  carbs: 25,
  description: 'Описание блюда'
}
```

### Добавление синонимов для поиска
1. Открыть `src/views/RecipeGeneratorPage.js`
2. Добавить запись в объект `synonyms` в функции `getProductSynonyms()`

---

## 📈 Производительность

### Оптимизации
- Использование localStorage для быстрого доступа к данным
- Ленивая загрузка компонентов
- Мемоизация вычислений
- Оптимизация алгоритма поиска рецептов

### Ограничения
- Максимум 20 продуктов в списке для поиска рецептов
- Максимум 15 продуктов для запуска поиска
- Данные хранятся только в браузере (не синхронизируются между устройствами)

---

## 🐛 Известные ограничения

1. **Нет синхронизации данных** — данные хранятся только в localStorage браузера
2. **Нет бэкенда** — все операции выполняются на клиенте
3. **Ограниченная база рецептов** — 100 рецептов (можно расширить)
4. **Нет реального AI** — поиск рецептов основан на алгоритме сопоставления

---

## 🔮 Возможные улучшения

1. Интеграция с реальным AI API для генерации рецептов
2. Синхронизация данных через облачное хранилище
3. Мобильное приложение
4. Экспорт планов в PDF
5. Интеграция с фитнес-трекерами
6. Социальные функции (обмен рецептами)
7. Расширенная аналитика прогресса

---

## 📄 Лицензия

Проект создан в образовательных целях.

---

## 👥 Авторы

Разработано для дипломной работы.

---

## 📞 Контакты

Для вопросов и предложений обращайтесь через форму обратной связи на сайте.

---

**Версия:** 1.0.0  
**Последнее обновление:** 2024

